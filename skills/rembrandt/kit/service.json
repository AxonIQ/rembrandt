{
  "host": "https://rembrandt-telemetry.vercel.app",
  "note": "Base URL of the Rembrandt service (telemetry collector and Slides delivery). Empty means: no telemetry, PPTX delivered as a file. The host must be on Axoniq's Anthropic network allowlist or the sandbox proxy refuses the request and the run falls back to the file. Environment variables REMBRANDT_TELEMETRY_URL and REMBRANDT_SLIDES_URL override it."
}
